package main

// AppVersion is bumped on every release. Keep it in sync with the git tag
// pushed to GitHub (e.g. tag "v1.0.0" -> AppVersion "1.0.0").
const AppVersion = "1.0.1"

const GithubRepo = "freeb5d/squish"
const GithubURL = "https://github.com/" + GithubRepo
